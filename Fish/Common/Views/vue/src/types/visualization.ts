// Data type for representing CSS absolute position properties
export interface CSSAbsolutePosition {
    top: string;
    left: string;
}

// Data type for representing CSS box sizing (height and width)
export interface CSSElementSize {
    height: string;
    width: string;
}